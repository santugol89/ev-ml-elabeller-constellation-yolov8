<telerik:RadWindow x:Class="GenieSupervisor.AddProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:GenieSupervisor"
        mc:Ignorable="d" Header="Add New Project" Background="Black" BorderThickness="2" BorderBrush="WhiteSmoke"
        Height="auto" Width="auto" WindowStartupLocation="CenterScreen"  ResizeMode="NoResize" CanClose="True" telerik:StyleManager.Theme="Green">

    <Grid Background="#FF686262">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,5,10,5">
            <Label Content="Enter Project Name " Margin="0,0,0,0" VerticalAlignment="Center" FontSize="14" Foreground="White"/>
            <TextBox x:Name="txtProjectName" HorizontalAlignment="Left" Height="25" Padding="0" VerticalAlignment="Center" Width="200" Foreground="Black" BorderBrush="Black" Margin="0"  FontSize="15" FontFamily="Segoe UI" PreviewKeyDown="txtProjectName_PreviewKeyDown"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,5">
            <Border Name="AddProject" Width="25" Height="25" Background="Purple" CornerRadius="50" PreviewMouseLeftButtonDown="AddProject_Click" Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right" >
                <materialDesign:PackIcon Name="packIconAddProject" Background="Purple" Foreground="White" Kind="ContentSave" Height="18" Width="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Border.ToolTip>
                    <ToolTip>
                        <StackPanel>
                            <TextBlock FontStyle="Italic" Text="Save Project" Foreground="Black"/>
                        </StackPanel>
                    </ToolTip>
                </Border.ToolTip>
            </Border>
        </StackPanel>

    </Grid>
</telerik:RadWindow>
